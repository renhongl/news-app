language: node_js

branches:
  only:
  - master

before_script:
  - npm install -g ionic cordova

script:
  - ionic cordova build browser - prod

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: master
  local_dir: www